{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "Jwt": {
        "Key": "vocab10", // Secret key
        "Issuer": "localhost", // Replace with your domain or app name
        "Audience": "localhost" // Replace with your app's audience
    }
}
